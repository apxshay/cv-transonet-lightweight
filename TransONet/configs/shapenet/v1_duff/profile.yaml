inherit_from: configs/shapenet/v1_duff/train.yaml

data:
  path: /dataset

training:
  out_dir: out/v1_duff/profile

test:
  model_file: /workspace/out/v1_duff/train/model_best.pt

generation:
  vis_n_outputs: 1
  generate_pointcloud: false
  copy_groundtruth: false
  copy_input: true

profiling:
  one_sample_per_category: true
